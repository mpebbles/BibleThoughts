Early TODOs
***********

Cleanup
Finish getting entries (styling, delete, view full entry, edit entry)
add all client side updates (redux logic)
pagination with content (using scroll detection to load more)
add search entries
implement API calls for edit/remove



landing page
icon
add limits to entries in database, sanitize
re-design tag portion of database if application ever needs to scale
